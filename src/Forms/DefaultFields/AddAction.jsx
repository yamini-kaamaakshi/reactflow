import {Button, Form} from "antd";
const AddAction = () => {
    return (
        <Form.Item>
            <Button type="primary" htmlType="submit">
              Add Action
            </Button>
        </Form.Item>
    );
};

export default AddAction